OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: comma-separated list of additional allowed CORS origins.
# The deployed Vercel frontend and common local dev ports are always allowed.
# Example: ALLOWED_ORIGINS=https://myapp.example.com,http://localhost:4000
ALLOWED_ORIGINS=
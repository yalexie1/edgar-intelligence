openai>=1.0
anthropic>=0.39
python-dotenv>=1.0
requests
beautifulsoup4
pinecone>=3.0
fastapi
uvicorn[standard]
slowapi
cohere>=5.0
pytest
httpx  # bench/load_test.py (v4 #2); previously only a transitive dep

# Optional — only needed for evals/eval_ragas.py. Works on Python 3.14 (the
# nest_asyncio/asyncio.current_task() incompatibility once thought to require
# 3.11/3.12 is worked around in eval_ragas.py itself — v4 #4, 2026-07-22).
# ragas>=0.1.9
# datasets>=2.14
# pandas>=2.0
